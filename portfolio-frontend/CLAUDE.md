@AGENTS.md

# Project Guidelines

1. Next.js App Router와 Tailwind CSS 기반으로 작성한다.
2. 웹 개발 입문자가 이해할 수 있도록 코드에 명확한 주석을 단다.
3. 컴포넌트는 `src/components/` 하위에 기능별/섹션별로 분리해서 작성한다.
4. 추후 Spring Boot REST API와 통신할 예정이므로 API 호출 로직은 모듈화한다 (예: `src/lib/api/` 등 별도 계층으로 분리).
